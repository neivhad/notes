# Before pushing make sure to commit changes

# Step 0/1: add remote alias
git remote add origin https://github.com/username/my-repository.git

# Step 0/2: set upstream and push
git push -u origin main

# Step 1: push
git push